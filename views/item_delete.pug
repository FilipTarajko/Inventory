extends layout

block content
    | Navigation: 
    a(href='/') Categories
    |  > 
    a(href=item.category.url) #{item.category.name}
    |  > 
    a(href=item.url) #{item.name}
    |  > 
    | #{title}
    #displaybar
        | This operation is irreversible. Do you want to continue?
        form(method='POST' action='')
            div.form-group
                input#id.form-control(type='hidden', name='id', required='true', value=item._id)
            button.btn.btn-danger(type='submit') DELETE ITEM